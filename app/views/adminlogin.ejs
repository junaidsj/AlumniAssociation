<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Administrator Login - Alumni Association</title>
  
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  
    <!-- Custom CSS -->
    <style>
        body {
            padding-top: 50px;
            background-image: url('https://img.freepik.com/premium-photo/blue-glass-with-white-square-that-says-blue_1290686-78332.jpg?w=740');
            font-family: 'Roboto', sans-serif;
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
            height: 100vh;
            margin: 0;
        }

        .container {
            max-width: 450px;
            width: 100%;
            padding: 30px;
            background-color: rgba(0, 0, 51, 0.85); /* Transparent dark blue */
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 51, 0.5); /* Adjusted shadow for better blending */
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .container:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 24px rgba(0, 0, 51, 0.7);
        }

        h1 {
            font-size: 28px;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 20px;
            text-align: center;
            text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.5); /* Slight text shadow for depth */
        }

        h2 {
            font-size: 22px;
            font-weight: 500;
            color: #66b2ff;
            margin-bottom: 25px;
            text-align: center;
        }

        .form-control {
            background-color: rgba(255, 255, 255, 0.1); /* Slight transparency for input fields */
            border: 1px solid #66b2ff;
            border-radius: 8px;
            color: #fff;
            font-size: 16px;
            height: 45px;
            padding: 10px 15px;
            margin-bottom: 20px;
            transition: border-color 0.3s ease;
        }

        .form-control:focus {
            border-color: #00ccff;
            outline: none;
        }

        .form-control::placeholder {
            color: #bbb; /* Lighter placeholder text */
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            font-size: 18px;
            font-weight: 500;
            padding: 12px;
            width: 100%;
            border-radius: 8px;
            color: #fff;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 91, 187, 0.3);
        }

        .or {
            text-align: center;
            margin: 20px 0;
            font-size: 16px;
            color: #aaa;
            position: relative;
        }

        .or::before, .or::after {
            content: '';
            position: absolute;
            top: 50%;
            width: 40%;
            height: 1px;
            background-color: #555;
        }

        .or::before {
            left: 0;
        }

        .or::after {
            right: 0;
        }

        @media (max-width: 767px) {
            .container {
                padding: 20px;
            }

            h1 {
                font-size: 24px;
            }

            h2 {
                font-size: 20px;
            }

            .btn-primary {
                font-size: 16px;
            }
        }
    </style>
</head>

<body>

    <div class="container">
        <h1>Welcome Admin</h1>
        <h2>Login to Your Account</h2>
        <form method="post" action="/adminhome">
            <div class="form-group">
                <input type="email" name="email" class="form-control" placeholder="Enter email" required>
            </div>
            <div class="form-group">
                <input type="password" name="userPassword" class="form-control" placeholder="Enter Password" required>
            </div>
            <button type="submit" class="btn btn-primary">Login</button>
        </form>
        <div class="or">OR</div>
        <form method="post" action="/adminregister">
            <button type="submit" class="btn btn-primary">New Admin Signup!</button>
        </form>
    </div>

    <!-- jQuery and Bootstrap JS -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

</body>

</html>
